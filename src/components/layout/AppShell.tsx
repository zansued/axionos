/**
 * layout/AppShell — Canonical layout wrapper using modular layout components.
 * Re-exports the root AppShell for consistency within the layout module.
 */
export { AppShell } from "@/components/AppShell";
